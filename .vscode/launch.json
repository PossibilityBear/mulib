{
    "version": "0.2.0",
    "configurations": [

        {
            "type": "lldb",
            "request": "launch",
            "name": "Debug Rust Program",
            "program": "${workspaceFolder}/target/debug/${workspaceFolderBasename}", // Adjust if your binary name differs
            "args": [],
            "cwd": "${workspaceFolder}",
            "sourceLanguages": ["rust"]
        }
    ]
}